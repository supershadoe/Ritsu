`use strict`;

/**
 * user reminders can be ephemeral msgs which wud be sent in the channel they create it (or in dms but with heavy ratelimit and cooldowns with a warning)

 * channel reminders wud be bound to a channel and remind those who subscribe to it but is also editable by others

 * server reminders create server events which anyone can subscribe and discord wud take care of it to 👌
 */
export {};
